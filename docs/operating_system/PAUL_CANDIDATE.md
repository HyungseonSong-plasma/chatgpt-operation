# Paul Successor Contract — Historical Activation Record

**Status:** ACTIVATED on 2026-09-20  
**Version:** Paul  
**Predecessor:** Calvin  
**Canonical owner:** `HyungseonSong-plasma/chatgpt-operation`

Paul is no longer a reserved successor. The live baseline is:

```text
docs/operating_system/versions/2026-09-20_paul.md
```

The current OS registry is:

```text
docs/operating_system/README.md
```

## Activation decision

Paul was activated when the operating architecture moved from rule-working-set optimization to:

```text
minimal essential common rules
+ deterministic reusable skills
+ consumer-local semantic/scientific authority
```

The activation includes:

- a centralized minimal essential-rule layer;
- a common-rule extraction across all current consumer repositories;
- a portable `session-bootstrap` skill;
- exact-revision Paul consumer binding;
- retirement of Calvin-era operating metrics from live initialization/routing.

## What was retained

Rules remain explicit where they own:

```text
authority
normative policy
domain semantics
scientific meaning
consumer-specific acceptance
irreducible judgment
skill activation/output interpretation
```

## What was delegated

Deterministic reusable procedure is delegated to skills, including bootstrap, state refresh, mutation mechanics, governed execution, and controller lifecycle/throughput.

## Metrics decision

The former candidate contract proposed quantitative operating metrics for comparing Calvin and Paul.

That requirement is retired.

Paul does not use numeric rule-application, rule-load, or interaction-efficiency metrics as live OS acceptance criteria. Historical Calvin-era metrics are preserved as archives only.

Scientific/runtime/performance metrics remain consumer-domain concerns and are unaffected.

## Consumer migration

A consumer uses Paul only after explicitly binding to an exact central revision where the registry reports:

```text
Current version name: Paul
State: ACTIVE
```

Consumers pinned to earlier Calvin revisions remain historical Calvin consumers until migrated.

## Preservation

This file remains only as a readable record that the reserved Paul successor contract was fulfilled and superseded by the activated Paul baseline.

Future OS evolution should start from the active registry and Paul baseline rather than reopening this candidate contract.
